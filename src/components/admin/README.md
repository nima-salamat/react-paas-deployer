# Admin logging panel

```jsx
import { LoggingHealthPanel } from "../components/admin";
// Mount in existing admin navigation / router:
// <Route path="/admin/logging" element={<LoggingHealthPanel />} />
```

API: staff JWT `GET .../service/admin/logging/health/`
